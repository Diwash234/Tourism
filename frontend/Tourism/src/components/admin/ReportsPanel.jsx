import{useEffect,useState}from"react";import adminApi from"../../api/adminApi";export default function ReportsPanel(){const[d,setD]=useState(null),[from,setFrom]=useState(""),[to,setTo]=useState("");const load=()=>adminApi.getReports({from:from||undefined,to:to||undefined}).then(({data})=>setD(data));const exportCsv=async()=>{const{data}=await adminApi.exportReports({from:from||undefined,to:to||undefined});const u=URL.createObjectURL(data),a=document.createElement("a");a.href=u;a.download="tourism-report.csv";a.click();URL.revokeObjectURL(u)};useEffect(load,[]);if(!d)return <p className="text-slate-400">Loading reports…</p>;return <div className="space-y-5"><div className="flex gap-2"><input type="date" className="input-field" value={from} onChange={e=>setFrom(e.target.value)}/><input type="date" className="input-field" value={to} onChange={e=>setTo(e.target.value)}/><button onClick={load} className="bg-amber-400 px-5 rounded-xl font-black">Apply range</button><button onClick={exportCsv} className="bg-emerald-600 text-white px-5 rounded-xl font-black">Export CSV</button></div>{Object.entries(d).filter(([k,v])=>!["range","trends"].includes(k)&&!Array.isArray(v)).map(([group,values])=><section key={group}><h2 className="text-white font-black capitalize mb-2">{group}</h2><div className="grid grid-cols-2 md:grid-cols-4 gap-3">{Object.entries(values).filter(([,v])=>!Array.isArray(v)&&typeof v!=="object").map(([k,v])=><div key={k} className="bg-slate-950 border border-slate-700 rounded-xl p-4"><p className="text-xs text-slate-500 uppercase">{k.replaceAll('_',' ')}</p><b className="text-2xl text-amber-300">{String(v)}</b></div>)}</div></section>)}<div className="grid lg:grid-cols-3 gap-3">{Object.entries(d.trends||{}).map(([name,items])=><section key={name} className="bg-slate-950 rounded-2xl p-4"><h3 className="text-white font-black capitalize">{name} trend</h3>{items.map(x=><div key={x.month} className="flex justify-between text-xs text-slate-300 border-t border-slate-800 py-2"><span>{new Date(x.month).toLocaleDateString(undefined,{year:'numeric',month:'short'})}</span><b>{x.count}</b></div>)}</section>)}</div><section className="bg-slate-950 rounded-2xl p-4"><h2 className="text-white font-black mb-2">Staff Activity</h2>{(d.staff_activity||[]).map(x=><div key={x.user_email} className="flex justify-between text-xs text-slate-300 border-t border-slate-800 py-2"><span>{x.user_email}</span><b>{x.actions} actions</b></div>)}</section><section><h2 className="text-white font-black mb-2">Top Viewed Destinations</h2>{d.destinations.top_viewed.map((x,i)=><div key={x.id} className="bg-slate-950 rounded-xl p-3 flex justify-between text-sm text-slate-200 mb-2"><span>{i+1}. {x.name}</span><b>{x.views_count} views</b></div>)}</section></div>}
